import { BrowserRouter } from "react-router-dom";
import RoutesConfig from "./routes/AppRoutes";

const App = () => (
  <BrowserRouter>
    <RoutesConfig />
  </BrowserRouter>
);

export default App;
